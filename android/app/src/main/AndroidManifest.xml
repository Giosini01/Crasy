<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Alle challenge si partecipa scattando sul momento oppure scegliendo
         una foto gia' fatta: servono tutte e due le strade. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:label="CRASY"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- **Il nome con cui l'app si fa chiamare da fuori.**

                 E' la meta' Android della stessa cosa dichiarata in
                 `ios/Runner/Info.plist`: senza, il tasto "Apri CRASY" della
                 pagina di conferma chiederebbe al telefono di aprire
                 `crasy://` e il telefono risponderebbe che non conosce
                 nessuno con quel nome.

                 `BROWSABLE` e' la categoria che conta: senza quella l'app
                 risulta raggiungibile da un'altra app ma **non da un
                 browser** — cioe' non dall'unico posto da cui serve, visto
                 che si arriva li' cliccando il link di un'email. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="crasy"/>
            </intent-filter>

            <!-- **I link condivisi, che aprono l'app invece del browser.**
                 
                 E' il meccanismo con cui un link di TikTok mandato su WhatsApp
                 apre TikTok: si chiamano App Links, e reggono su due
                 dichiarazioni che devono combaciare — questa, e il file
                 `assetlinks.json` servito da crasyapp.com, che contiene
                 l'impronta della chiave con cui l'app e' firmata. Se una delle
                 due manca o non corrisponde, il telefono apre il browser e non
                 dice perche'.

                 **Solo `/foto`, non tutto il dominio.** Il sito e' una vetrina
                 e deve restare un sito: l'unico indirizzo che appartiene
                 all'app e' quello di una foto in gara.

                 `autoVerify` e' cio' che fa aprire l'app **senza chiedere
                 niente**. Senza, Android mostrerebbe ogni volta la scelta fra
                 browser e app — che e' peggio di non averlo fatto. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="crasyapp.com"
                    android:pathPrefix="/foto"/>
                <data
                    android:scheme="https"
                    android:host="www.crasyapp.com"
                    android:pathPrefix="/foto"/>
            </intent-filter>

            <!-- Senza questo, i link entrano nell'app ma non arrivano al
                 router: Flutter li lascia cadere e si atterra sulla home come
                 se si fosse aperta l'app da sola. -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="true" />
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
